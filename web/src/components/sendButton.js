

//전송 버튼
function sendButton(props) {
    return (
        <div>
            <button onClick={() => {
                this.setState({number: number + 1});
                }}
            >
            </button>
        </div>
    );
}


export default sendButton;